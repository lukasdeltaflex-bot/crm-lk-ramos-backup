# Configurações para o Firebase App Hosting
# https://firebase.google.com/docs/app-hosting/configure

buildConfig:
  runtime: nodejs20

runConfig:
  # Memória e CPU aumentados para evitar erro 500 no boot do Next.js 15
  memory: 1024Mi
  cpu: 1
  maxInstances: 1