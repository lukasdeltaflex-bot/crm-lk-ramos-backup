
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ABERTURA TOTAL TEMPORÁRIA PARA DEBUG OBRIGATÓRIO
    // Se o erro persistir aqui, o problema é o Project ID ou variáveis de ambiente.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
